# RPM signing key

Place the ASCII-armored public RPM signing key in this directory as:

```text
RPM-GPG-KEY-nginx-modules-rpm
```

The public key is committed and attached to every GitHub release. Never place
the private signing key in this directory or anywhere else in the repository.
